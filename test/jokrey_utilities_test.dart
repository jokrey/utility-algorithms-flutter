import 'package:flutter_test/flutter_test.dart';

void main() {
  test('server not implemented in flutter, due to would be uselessness', () {
    //no network tests available
    expect(true, true);
  });
}
